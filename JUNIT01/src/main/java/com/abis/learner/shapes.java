package com.abis.learner;
 // In TDD approach first we need to preapre method without declaration then create
 // test after that need to declare methods according to test requirement

public class shapes {
    public double computeAreaOfSquare(double side)
    {
        return side * side;
    }
    public double computeAreaOfCircle(double radius)
    {
        return 3.14 * radius * radius;
    }
}
